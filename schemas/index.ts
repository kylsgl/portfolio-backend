import contact from './contact';
import hero from './hero';
import links from './links';
import projects from './projects';

export const orderableItemTypes: string[] = ['links', 'projects'];

export const schemaTypes = [contact, hero, links, projects];
